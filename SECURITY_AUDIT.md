# 🛡️ تقرير الفحص الأمني الشامل ومشكلات الأداء للمشروع (Hirafi Security Audit)

تاريخ الفحص: 2026-09-11  
حالة المشروع: مراجعة شاملة لجميع الملفات والمسارات (API & Security Review)

---

## 📋 ملخص التقرير التنفيذي
تم فحص جميع ملفات المشروع سطرًا بسطر (المصادقة، المسارات الإدارية، نظام الرسائل، الجلسات، وقواعد البيانات).
تم تقسيم الملاحظات والثغرات إلى **عالية الخطورة**، **متوسطة الخطورة**، و**تحسينات الأداء والأمان المستقبلي**.

---

## 🚨 1. الثغرات عالية الخطورة (Critical / High Risk)

### 1.1 نقاط النهاية التجريبية المفتوحة للعامة (Exposed Debug Endpoints)
- **الملف:** `src/app/api/fix-db/route.ts`
  - **المشكلة:** يتيح تنفيذ أوامر SQL غير آمنة (`$executeRawUnsafe`) لتعديل اعمدة قاعدة البيانات بدون أي تحقق من هوية المستخدم (Authentication Check).
  - **الخطورة:** يمكن لأي شخص في الإنترنت زيارة `/api/fix-db` وتعديل جدول المستخدمين.
  - **التوصية العلاجية:** حذف الملف بالكامل في بيئة الإنتاج أو حمايته بشرط `session.role === "ADMIN"`.

- **الملف:** `src/app/api/admin/make-me-admin/route.ts`
  - **المشكلة:** يتيح لأي مستخدم مسجل الدخول (حرفي أو زبون) ترقية نفسه فوراً إلى دور `ADMIN` بنقرة واحدة بمجرد إرسال طلب لـ POST/GET على هذا المسار.
  - **الخطورة:** رفع الصلاحيات غير المصرح به (Privilege Escalation).
  - **التوصية العلاجية:** إزالة هذا المسار تماماً بعد اعتماد حساب المشرف الرئيسي.

- **الملف:** `src/app/api/admin/setup/route.ts`
  - **المشكلة:** يعيد ضبط كلمة سر الحساب `admin@hirafi.dz` إلى كلمة السر الافتراضية المعلنة دون أي تحقق من هوية الطالب.
  - **الخطورة:** إمكانية إعادة تعيين حساب الإدارة من قبل أي شخص على الإنترنت.
  - **التوصية العلاجية:** حذف المسار وإدارة الحسابات الإدارية عبر قواعد البيانات أو ملحقات CLI (Seed scripts).

- **الملف:** `src/app/api/debug-env/route.ts`
  - **المشكلة:** يعرض بيانات عن الاتصال بقاعدة البيانات وأسماء متغيرات البيئة ورسائل الخطأ التفصيلية (Stack Traces).
  - **الخطورة:** تسريب معلومات النظام (Information Disclosure).
  - **التوصية العلاجية:** حذف الملف أو تعطيله في بيئة الإنتاج.

---

### 1.2 ثغرة فحص الصلاحيات في نظام الرسائل (Insecure Direct Object Reference - IDOR)
- **الملف:** `src/app/api/messages/[id]/route.ts`
  - **المشكلة:** في دالتي `GET` و `POST` الخاصة بجلب وإرسال الرسائل لمحادثة محددة بـ `id`:
    - يتم التحقق فقط من وجود جلسة (`session.userId`).
    - **لا يتم التحقق** مما إذا كان المستخدم الحالي (`session.userId`) هو بالفعل طرف في المحادثة (`clientId` أو `artisanId`).
  - **الخطورة:** يمكن لأي مستخدم مسجل قراءة رسائل محادثات الآخرين أو التجسس عليها أو التعديل عليها بمجرد معرفة رمز المحادثة (`id`).
  - **التوصية العلاجية:** إضافة فحص صلاحيات يضمن أن `session.userId === conversation.clientId || session.userId === conversation.artisanId`.

---

## ⚠️ 2. المشكلات متوسطة الخطورة (Medium Risk)

### 2.1 غياب حماية OAuth CSRF (State Parameter in Google Auth)
- **الملفات:** `src/app/api/auth/google/route.ts` و `src/app/api/auth/google/callback/route.ts`
  - **المشكلة:** عند توجيه المستخدم لتسجيل الدخول عبر Google، لا يتم استخدام معامل `state` (Anti-CSRF Token).
  - **الخطورة:** هجمات ربط الحسابات القسري (OAuth Account Linking Attack).
  - **التوصية العلاجية:** توليد رمز عشوائي في الكوكي والتحقق منه في مسار الـ Callback.

### 2.2 الحسابات الوهمية والإغراق (No Rate Limiting & Bot Protection)
- **المشكلة:** غياب محدد معدل الطلبات (Rate Limiter) على مسارات:
  - تسجيل الدخول `/api/auth/login` (عرضة لهجمات التخمين Brute-Force).
  - إنشاء الحسابات `/api/auth/register` (عرضة لإنشاء حسابات وهمية بكميات ضخمة Bots).
  - إرسال الرسائل والتقييمات.
- **التوصية العلاجية:** إضافة مكتبة حظر الإغراق (مثل `@upstash/ratelimit` أو Rate Limiter Middleware) وحماية التسجيل بـ CAPTCHA أو تأكيد البريد الإلكتروني.

### 2.3 عدم إبطال الجلسة عند تغيير حالة المستخدم (Session Revocation)
- **الملف:** `src/lib/session.ts`
  - **المشكلة:** الجلسة تُخزن كـ JWT/Signed Cookie وتدوم 7 أيام. إذا تم حظر المستخدم أو تعديل دوره من قِبل الأدمن، يظل التوكن الصادر سابقاً صالحاً حتى انتهاء مدته ما لم يتم التحقق الدائم من قاعدة البيانات عند المسارات الحساسة.
- **التوصية العلاجية:** إضافة فحص سريع لحالة المستخدم في قاعدة البيانات عند طلب المسارات الإدارية الحساسة.

---

## 💡 3. تحسينات جودة البيانات والأداء (Performance & Quality Improvements)

1. **التحقق من قيم التقييمات (Input Validation):**
   - في `/api/reviews/route.ts` يجب التأكد من أن التقييم (Rating) عدد صحيح ينحصر بين 1 و 5 فقط.

2. **الاستعلامات المتكررة (Polling Efficiency):**
   - جرس الإشعارات والرسائل يقوم بجلب البيانات كل 10 ثوانٍ. يفضل استخدام WebSockets (مثل Pusher أو Supabase Realtime) مستقبلاً لتقليل استهلاك السيرفر وقاعدة البيانات.

3. **الكلمات المفتاحية وكود المسار الأصلي:**
   - نقل كلمة السر الافتراضية والتأكد من تغيير كافة المفاتيح السرية في ملف `.env` السحابي على Vercel لضمان أقصى حماية.

---

## 🎯 توصيات الخطوات القادمة
1. مراجعة نقاط النهاية المذكورة في هذا التقرير.
2. غلق الأبواب التجريبية (`fix-db`, `make-me-admin`, `setup`, `debug-env`).
3. تعزيز فحص الملكية في مسارات الرسائل IDOR.
