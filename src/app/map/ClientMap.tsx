"use client";

// This file is kept for backward compatibility
// The new PublicMapClient.tsx handles the full map page now
export default function ClientMap({ artisans, center }: { artisans: any[], center: [number, number] }) {
  return null;
}
